---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  labels:
    app: "deployment-storage-emptydir"
  name: "deployment-storage-emptydir"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "deployment-storage-emptydir"
  strategy: {}
  template:
    metadata:
      labels:
        app: "deployment-storage-emptydir"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 6464
      containers:
        - image: "busybox:latest"
          name: "busybox"
          resources:
            requests:
              cpu: "10m"
              memory: "24Mi"
            limits:
              cpu: "20m"
              memory: "32Mi"
          securityContext:
            readOnlyRootFilesystem: true
          volumeMounts:
            - mountPath: "/volume"
              name: "emptydir-volume"
          command:
            - "/bin/sh"
            - "-c"
            - "while true; do echo 'hello world'; sleep 10; done"
      volumes:
        - name: "emptydir-volume"
          emptyDir:
            sizeLimit: 500Mi
...
